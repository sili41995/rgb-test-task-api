const enum ErrorMessages {
  titleReqErr = 'Missing required title field',
  titleStringErr = 'Title must be a string',
  textReqErr = 'Missing required text field',
  textStringErr = 'Text must be a string',
  duplicatePostErr = 'Post already use',
  postNotFound = 'Post was not found',
}

export default ErrorMessages;
